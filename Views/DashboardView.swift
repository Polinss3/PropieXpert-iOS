import SwiftUI

struct DashboardView: View {
    var body: some View {
        VStack {
            Text("Dashboard")
                .font(.largeTitle)
                .padding()
            Spacer()
        }
    }
}

#Preview {
    DashboardView()
} 