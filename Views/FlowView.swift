import SwiftUI

struct FlowView: View {
    var body: some View {
        VStack {
            Text("Flujo de Ingresos y Gastos")
                .font(.largeTitle)
                .padding()
            Spacer()
        }
    }
}

#Preview {
    FlowView()
} 