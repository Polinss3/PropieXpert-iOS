import SwiftUI

struct PropertiesView: View {
    var body: some View {
        VStack {
            Text("Propiedades")
                .font(.largeTitle)
                .padding()
            Spacer()
        }
    }
}

#Preview {
    PropertiesView()
} 