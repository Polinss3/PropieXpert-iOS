import SwiftUI

struct IncomeView: View {
    var body: some View {
        VStack {
            Text("Ingresos")
                .font(.largeTitle)
                .padding()
            Spacer()
        }
    }
}

#Preview {
    IncomeView()
} 