import SwiftUI

struct PlaceholderView: View {
    var body: some View {
        VStack {
            Text("Más próximamente")
                .font(.largeTitle)
                .padding()
            Spacer()
        }
    }
}

#Preview {
    PlaceholderView()
} 