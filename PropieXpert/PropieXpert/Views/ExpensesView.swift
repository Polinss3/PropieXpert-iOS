import SwiftUI

struct ExpensesView: View {
    var body: some View {
        VStack {
            Text("Gastos")
                .font(.largeTitle)
                .padding()
            Spacer()
        }
    }
}

#Preview {
    ExpensesView()
} 